package com.Elements;

import org.openqa.selenium.By;
import org.openqa.selenium.WebElement;

public class Image extends BaseElement {

    public Image(WebElement elem) {
        super(elem);
    }
}
